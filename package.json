{
  "name": "dashboard-influencer-marcas",
  "version": "1.0.0",
  "private": true,
  "description": "Dashboard de impacto de anuncios multi-plataforma para influencer",
  "scripts": {
    "build:css": "tailwindcss -i ./styles.css -o ./dist/app.css --minify",
    "watch:css": "tailwindcss -i ./styles.css -o ./dist/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4",
    "tailwindcss": "^4"
  }
}
